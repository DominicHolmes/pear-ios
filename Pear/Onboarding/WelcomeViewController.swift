//
//  WelcomeViewController.swift
//  Pear
//
//  Created by Dominic Holmes on 1/24/18.
//  Copyright © 2018 Dominic Holmes. All rights reserved.
//

import UIKit
import FirebaseDatabase

class WelcomeViewController: UIViewController {
    
    // TEST CODE - - -
    
    /*@IBAction func create() {
        let userInfo = UserInfo(id: "testId", username: "hdom", nameFirst: "Dominic", nameLast: "Holmes")
        UserNetworkingManager.shared.createUser(from: userInfo) { (success, user) in
            dump(user)
        }
    }
    
    @IBAction func getUser() {
        UserNetworkingManager.shared.fetchUser(for: "testId") { (success, user) in
            print(success)
            dump(user)
        }
        UserNetworkingManager.shared.fetchUser(for: "testId2") { (success, user) in
            print(success)
            dump(user)
        }
    }
    
    @IBAction func update() {
        // cant update username
        let userInfo = UserInfo(id: "testId", username: "hdom2", nameFirst: "Dominic2", nameLast: "Holmes2")
        UserNetworkingManager.shared.updateUser(from: userInfo) { (success, user) in
            dump(user)
        }
    }
    
    @IBAction func delete() {
        UserNetworkingManager.shared.deleteUser(with: "testId") { (success) in
            dump(success)
        }
    }*/
    
    // END - - -
    
    
}
